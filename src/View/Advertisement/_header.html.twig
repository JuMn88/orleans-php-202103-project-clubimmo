<div class="container col-12 p-0">
    <div class="row col-12">

        <div class="col-12">
            <a class='link-dark fw-bold back'href="/Property/index">Revenir en arrière</a>
        </div>
        <div class="col-12 px-4 mt-5">
            {% if property is not null %}
                <div class="col-12">
                    <h2 class="fw-bold">{{ property.name }}, {{ sector.name }}</h2>
                </div>
                <div>
                    <h3>
                        {{ property.transaction }} {{ property.name }}, {{ propertyFeatures[0].number }} {{ propertyFeatures[0].name }},
                        {{ propertyFeatures[7].number }} {{ propertyFeatures[7].name }}, surface : {{ propertyFeatures[1].number }} {{ propertyFeatures[1].name }}
                    </h3>
                </div>
            {% endif %}
        </div>
    </div>
</div>
